package practice;


class practice{
    
    public void m1(){
        int a=10;
        int b=20;
        System.out.println("m1 says "+ a+b);
                
    }
}

 class test extends practice{

    public static void main(String [] args){
        test t=new test();
        t.m1();
    } 
} 

         
        
   
    
    


    
//system is a class present in java.lang.package
   //out is static variable printstream type
   //print is the method present in printstream class
/*normal import Explicit 
syntax:
import packagename,class name;
static import Explicit
import static packagename,class name,static member
normal implicit import
import packagename.class name;
staatic import 
import static packname.classname.static member
 
                     PACKAGES
it is a an encapsulation mechanism to group related classes and interafaces into a single unit whiches nothing but package
it is a an encapsulation mechanism to group related classes and interfaces  into a single unit whiches nothun but package
it is a an encapsulation mechanism to group related classes and interfaces into a single unit whiches nothing but package
it is a an encapsulation machanism to group related classes and interfaces into a single unit whiches nothing but package
it is a an encapsulation mechanism to group related classes and unterface into a single unit whiches nothing but package
*/

 
   
